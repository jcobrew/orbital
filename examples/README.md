Example JSON files land here per stream.

Each stream that defines a new data shape (e.g. ApplicationWindow, SourceRecord, FounderNeedsProfile,
ProposedProgramUpdate) should add a representative example JSON file alongside the schema it conforms to.
These examples serve as living documentation and can be used in tests.
